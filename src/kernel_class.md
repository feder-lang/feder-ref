## Classes

Classes implemented into the kernel:

```
mod std
    class{Type} Box(Var : Type)
        func *:
            Var;
    ;
    
    class{Type} MutBox(var : Type)
        func *:
            var;
    ;
;
```
